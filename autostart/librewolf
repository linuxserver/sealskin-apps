# Handle file/URL opening
ARGS=()
if [ -n "$SEALSKIN_FILE" ]; then
    ARGS+=("$SEALSKIN_FILE")
elif [ -n "$SEALSKIN_URL" ]; then
    ARGS+=("$SEALSKIN_URL")
fi

librewolf ${LIBREWOLF_CLI} "${ARGS[@]}"
