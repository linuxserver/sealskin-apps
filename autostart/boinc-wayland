while [ ! -f /config/all_projects_list.xml ]; do
    sleep 1
done
while ! grep -q rosetta /config/all_projects_list.xml; do
    sleep 1
done

cd /config

/usr/bin/boincmgr ${SEALSKIN_FILE:+"$SEALSKIN_FILE"} ${SEALSKIN_URL:+"$SEALSKIN_URL"}
