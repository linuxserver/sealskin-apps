rm -f $HOME/.config/darktable/library.db.lock
rm -f $HOME/.config/darktable/data.db.lock

# Handle file opening
ARGS=()
if [ -n "$SEALSKIN_FILE" ]; then
    ARGS+=("$SEALSKIN_FILE")
fi

xterm -e darktable "${ARGS[@]}"
